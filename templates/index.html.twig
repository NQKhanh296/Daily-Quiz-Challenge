<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kvízová Výzva</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <section id="start-screen" class="card">
            <h1>Denní Kvíz</h1>
            <p>Vyberte si obtížnost:</p>
            <div class="difficulty-select">
                <button class="btn-opt" onclick="startQuiz('easy')">Lehká</button>
                <button class="btn-opt" onclick="startQuiz('medium')">Střední</button>
                <button class="btn-opt" onclick="startQuiz('hard')">Těžká</button>
            </div>
            <button class="btn-secondary">🏆 Žebříček</button>
        </section>

        <section id="quiz-screen" class="card hidden">
            <div class="quiz-header">
                <span id="question-number">Otázka 1/3</span>
                <span id="timer">Čas: 0s</span>
            </div>
            <h2 id="question-text">Načítám otázku...</h2>
            <div id="answer-buttons" class="answer-grid"></div>
            <div class="progress-bar">
                <div id="progress-fill"></div>
            </div>
        </section>

        <section id="result-screen" class="card hidden">
            <h2>Kvíz dokončen!</h2>
            <div class="stats">
                <p>Body: <strong id="final-score">0</strong></p>
                <p>Čas: <strong id="final-time">0</strong>s</p>
                <p>Bonus za rychlost: <strong id="speed-bonus">0</strong></p>
            </div>
            <button onclick="location.reload()" class="btn-primary">Hrát znovu</button>
        </section>
    </div>
    <script src="js/script.js"></script>
</body>
</html>
{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
{% endblock %}

{% block body %}
<div class="container">
    <h1>Daily Quiz Challenge</h1>

    <div id="authScreen" class="screen">
        <h2>Vítej v Challenge</h2>
        <p style="text-align:center; color: #94a3b8;">Získej nový kód nebo se vrať ke svému účtu.</p>

        <div id="wordDisplay" class="word-box">
            <span class="word">...</span>
            <span class="word">...</span>
            <span class="word">...</span>
        </div>

        <button onclick="generateNewWords()" id="genBtn">Vygenerovat jiná slova</button>

        <div class="divider">nebo zadej svůj kód</div>

        <input type="password" id="manualWords" placeholder="slovo1 slovo2 slovo3">

        <button class="primary" onclick="authenticateUser()">Vstoupit do kvízu</button>

        <p class="small" style="text-align:center; margin-top:15px; color:#64748b; font-size: 0.8rem;">
            Slova slouží jako váš unikátní login.
        </p>
    </div>

    <div id="startScreen" class="screen hidden">
        <h2>Dnešní výzva</h2>
        <p id="todayTopic">Téma: <span>Načítání...</span></p>

        <div class="buttons">
            <button onclick="startQuiz(1)">Lehká</button>
            <button onclick="startQuiz(2)">Střední</button>
            <button onclick="startQuiz(3)">Těžká</button>
        </div>

        <div class="divider">nebo se podívej na ostatní</div>
        <button onclick="showLeaderboard()" style="background: #475569;">Zobrazit žebříček</button>
    </div>

    <div id="quizScreen" class="screen hidden">
        <div class="header">
            <span>Otázka <span id="questionNum">1</span>/3</span>
            <span> <span id="timer">0</span>s</span>
        </div>

        <h2 id="questionText">Načítám otázku</h2>

        <div id="answers"></div>

        <div class="controls" style="margin-top: 20px;">
            <button id="nextBtn" class="hidden" onclick="handleNextStep()">Další otázka</button>
        </div>

        <p style="text-align:center;">Skóre: <span id="score">0</span></p>
    </div>

    <div id="resultScreen" class="screen hidden">
        <h2>Hotovo!</h2>
        <p style="font-size: 2rem; color: #6366f1;">Tvoje skóre: <span id="finalScore">0</span></p>
        <p style="text-align:center;">Čas: <span id="finalTime">0</span>s</p>
        <button class="primary" onclick="showLeaderboard()">Zobrazit žebříček</button>
    </div>

    <div id="leaderboardScreen" class="screen hidden">
        <h2>Žebříček TOP 10</h2>

        <div class="current-user-stats">
            <strong>Tvůj výsledek:</strong> <span id="playerCurrentAttempt">0</span> bodů (<span id="playerCurrentTime">0</span>s)
        </div>

        <ul id="leaderboardList" class="leaderboard-list"></ul>

        <div class="buttons">
            <button class="primary" onclick="showScreen('startScreen')">Zpět do menu</button>
            <button onclick="logout()">Odhlásit se</button>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/script.js') }}"></script>
{% endblock %}
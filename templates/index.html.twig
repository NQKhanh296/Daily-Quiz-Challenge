{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1>Daily Quiz Challenge</h1>

    <div id="startScreen" class="screen">
        <h2>Dnešní výzva</h2>
        <p id="todayTopic">Téma: <span>Načítání...</span></p>

        <div class="buttons">
            <button onclick="startQuiz(1)">Lehká</button>
            <button onclick="startQuiz(2)">Střední</button>
            <button onclick="startQuiz(3)">Těžká</button>
        </div>
    </div>

    <div id="quizScreen" class="screen hidden">
        <div class="header">
            <span>Otázka <span id="questionNum">1</span>/3</span>
            <span> <span id="timer">0</span>s</span>
        </div>

        <h2 id="questionText">Načítám otázku</h2>

        <div id="answers"></div>

        <p>Skóre: <span id="score">0</span></p>
    </div>

    <div id="resultScreen" class="screen hidden">
        <h2> Hotovo!</h2>
        <p class="big">Tvoje skóre: <span id="finalScore">0</span></p>
        <p>Čas: <span id="finalTime">0</span>s</p>
        <button onclick="showLeaderboard()">Zobrazit žebříček</button>
    </div>

    <div id="leaderboardScreen" class="screen hidden">
        <h2>Žebříček</h2>
        <p>Žebříčky budou dostupné brzy.</p>
        <button onclick="location.reload()">Zpět na start</button>
    </div>
</div>
{% endblock %}